using GeometryUtils.Shapes;

namespace GeometryUtils.Utils
{
    public static class GeometryCalculator
    {
        public static double CalculateArea(Shape shape) => shape.CalculateArea();
    }
}
